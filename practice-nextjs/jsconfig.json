{
  "compilerOptions": {
    // compilerOptions 역할: TypeScript 컴파일러 옵션을 설정하는 객체입니다.
    "paths": {
      // paths 역할: 코드에서 긴 경로를 간단하게 표현할 수 있도록 도와줍니다.
      "@/*": ["./*"]
      // 왼쪽 : @/로 시작하는 경로를 별칭으로 사용합니다. *는 와일드카드로, 모든 경로를 포함합니다.
      // 오른쪽 : 실제 파일 시스템 경로를 지정합니다. "./*"는 현재 프로젝트의 루트 디렉토리를 나타냅니다.
    }
  }
}
